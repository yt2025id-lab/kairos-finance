# Kairos Finance CRE Workflow Secrets
# These secrets are securely managed by the CRE runtime
# and accessed via runtime.getSecret() in the workflow

secrets:
  ANTHROPIC_API_KEY:
    description: "Anthropic API key for Claude AI yield analysis"
    env: ANTHROPIC_API_KEY
