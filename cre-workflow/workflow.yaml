# Kairos Finance CRE Workflow Configuration
# Deploys to Chainlink DON for AI-powered yield optimization on Base

targets:
  staging-settings:
    workflow-name: kairos-yield-optimizer-staging
    workflow-path: ./main.ts
    config-path: config.staging.json
    secrets-path: secrets.yaml

  production-settings:
    workflow-name: kairos-yield-optimizer
    workflow-path: ./main.ts
    config-path: config.json
    secrets-path: secrets.yaml
